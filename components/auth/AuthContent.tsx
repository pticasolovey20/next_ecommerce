import Button from "@/ui/Button";

const AuthContent = () => {
  return (
    <div>
      <Button size="lg" variant="primary">
        Login with Google
      </Button>
    </div>
  );
};

export default AuthContent;
